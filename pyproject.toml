[project]
name = "visionlab"
version = "0.1.0"
description = "Computer vision / ML araştırma ve deney projesi"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
